version: "3.9"

services:
  contract-api:
    build: .
    container_name: contract_extraction_service
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
